version: "3.9"
services:
  app:
    restart: always
    build:
      context: ./
      target: dev
      dockerfile: Dockerfile
    volumes:
        - .:/app
        - /app/node_modules
        - /app/.next
    ports:
      - "3000:3000"
  redis:
    image: fesvd:1.0